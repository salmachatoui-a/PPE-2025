Pour créer mon premier repository je suis allé dans mon terminal puis j'ai " mkdir Journal-de-bord-individuel-PPE"
mkdir = commande unix pour creer des repertoire ( ou dossier).
j'ai executé la commande Ls pour pouvoir voir mon nouveau repertoire.

Maintenant j'excuse ligne par ligne ce que github veut que je commande dans mon terminal afin syncroniser le repositository

 cd Journal-de-bord-individuel-PPE/ = la commande Cd me permet ici d'entrer dans mon dossier.
  echo "# Journal-de-bord-individuel-PPE" >> README.md
echo  me permet d'afficher  ce qui suit ""  >> = premet d'ajouter a la liste  README.md = readme = nom du fichier .md ( mark down) format d'affichage.
git init = cette commande me permet de creer un nouveau depot git (Elle permet de convertir un projet existant non versionné en dépôt Git ou d'initialiser un nouveau dépôt vide.
La plupart des autres commandes Git ne sont disponibles qu'à partir d'un dépôt initialisé ; c'est donc généralement la première commande que vous exécuterez dans un nouveau projet. source altassian)
 git add README.md 
git commit -m "first commit" =  à cette etape j'ai saisie mon email et mon identifiant comme cela m'a ete demandé.
git branch -M main =  
ssh -T git@github.com
# Attempts to ssh to Github
